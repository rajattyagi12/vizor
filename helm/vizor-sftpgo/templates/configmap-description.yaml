{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: vizor-sftpgo-description
  namespace: {{ .Release.Namespace }}
data:
  description.json: |
    {{- .Values.description | nindent 4 }}
{{- end }}
