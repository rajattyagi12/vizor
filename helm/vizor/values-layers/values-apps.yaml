components:
  foundation:
    enabled: false
  dataInit:
    enabled: false
  identity:
    enabled: false
  platformSupport:
    enabled: false
  apps:
    enabled: true
  trafficAutoscale:
    enabled: false

# Create the same SA as foundation (same name via Argo param) so it exists if foundation hasn't synced yet.
serviceAccount:
  create: true

local-path-provisioner:
  enabled: false
