components:
  foundation:
    enabled: true
  dataInit:
    enabled: false
  identity:
    enabled: false
  platformSupport:
    enabled: false
  apps:
    enabled: false
  trafficAutoscale:
    enabled: false
# Redis is deployed by the standalone vizor-redis Application (wave -3). Dapr redisHost for vizor-foundation is derived from that app (env.redisServiceName:6379) via the root ApplicationSet templatePatch.
