components:
  foundation:
    enabled: true
  dataInit:
    enabled: false
  identity:
    enabled: false
  apps:
    enabled: false
  trafficAutoscale:
    enabled: false
# Redis is configured in values-layers/values-redis.yaml and loaded only by vizor-foundation (extraValueFile)
