components:
  foundation:
    enabled: true
  dataInit:
    enabled: false
  identity:
    enabled: false
  apps:
    enabled: false
  trafficAutoscale:
    enabled: false
# Redis is deployed by the standalone vizor-redis Application (wave -3), not by this chart.
