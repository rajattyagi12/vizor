components:
  foundation:
    enabled: false
  dataInit:
    enabled: false
  identity:
    enabled: true
  platformSupport:
    enabled: false
  apps:
    enabled: false
  trafficAutoscale:
    enabled: false

# Do not create SA here; use the one from foundation (name from Argo param serviceAccount.name or "default").
serviceAccount:
  create: false

local-path-provisioner:
  enabled: false
