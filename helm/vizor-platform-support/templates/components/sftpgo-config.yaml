{{- if .Values.sftpgo.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: vizor-sftpgo-config
  namespace: {{ .Release.Namespace }}
data:
  config.yml: |
{{ .Values.sftpgo.config | indent 4 }}
{{- end }}
